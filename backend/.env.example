# ===================================
# FLOWMATIC BACKEND ENVIRONMENT VARIABLES
# ===================================

# Server Configuration
NODE_ENV=development
PORT=5000
BASE_PATH=/api

# Database Configuration
# For local MongoDB:
MONGO_URI=mongodb://localhost:27017/flowmatic
# For MongoDB Atlas (replace with your connection string):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/flowmatic?retryWrites=true&w=majority

# JWT Configuration (REQUIRED)
# Generate a secure random string for production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-make-it-very-long-and-random-123456789
JWT_EXPIRES_IN=7d

# Session Configuration (REQUIRED for Google OAuth)
# Generate a secure random string for production
SESSION_SECRET=your-super-secret-session-key-here-make-it-long-and-random-987654321

# Google OAuth Configuration (Optional - only if using Google login)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# Frontend Configuration
FRONTEND_ORIGIN=http://localhost:5173
CLIENT_URL=http://localhost:5173
FRONTEND_GOOGLE_CALLBACK_URL=http://localhost:5173/auth/google/callback

# Email Configuration (Optional - for forgot password, notifications, etc.)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Security & Rate Limiting (Optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Development/Debug
DEBUG=true

# ===================================
# IMPORTANT NOTES:
# ===================================
# 1. Copy this file to .env (without .example extension)
# 2. Replace all placeholder values with actual values
# 3. Never commit .env file to version control
# 4. JWT_SECRET and SESSION_SECRET must be long random strings
# 5. For production, use environment-specific values
# ===================================
